---
description: Deprecated — renamed to /invoices. Delegates to commands/invoices.md.
---

# /generate-invoices (deprecated alias)

> **This command has been renamed to `/invoices`.** `/generate-invoices` still works but will
> be removed in a future version — use `/invoices` going forward.

Delegate the entire request, including any arguments (e.g. a target month like
`/generate-invoices 2026-04`), to `commands/invoices.md` and follow that command's
workflow exactly from Step 0 onward. Do not duplicate the logic here.
